# Client (Vite + React + Tailwind) - Dark Theme

## Setup
cd client
npm install
npm run dev

By default the client calls http://localhost:5000/extract.
To change server URL, set environment variable VITE_SERVER_URL in a .env file at client root.

Example .env:
VITE_SERVER_URL=https://your-server.example.com
